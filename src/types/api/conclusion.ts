export type TAddConclusion = {
  code: string;
  category: string;
  description: string;
};

export type TEditConclusion = {
  id: number;
  code: string;
  category: string;
  description: string;
};
