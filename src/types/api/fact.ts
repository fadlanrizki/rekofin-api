export type TAddFact = {
  code: string;
  description: string;
  question: string;
  fact: string;
};

export type TEditFact = {
  id: number;
  code: string;
  description: string;
  question: string;
  fact: string;
};
